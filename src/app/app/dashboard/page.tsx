const DashboardPage = () => {
    return (
        <div>
            <h1>Dashboard</h1>
        </div>
    );
};

const Dashboard = () => {
    return <DashboardPage />;
};

export default Dashboard;
